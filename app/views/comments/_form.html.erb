<%= form_for([@restaurant, @restaurant.comments.build]) do |f| %>
	<p>
		
		<%= f.hidden_field :user_id, value: current_user %>
		
	</p>

	<p>
		<%= f.label :body %><br>
		<%= f.text_area :body %>
	</p>
	<p>
		<%= f.submit %>
	</p>
<% end %>